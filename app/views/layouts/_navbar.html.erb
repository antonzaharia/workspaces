<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">
      Workspaces
    </a>
    <div class="d-flex flex-nowrap">
      <% if user_signed_in? %>
        
        <div class="nav-item dropdown">
          <div class="account-avatar dropdown-toggle" id="navbarDropdown" data-bs-toggle="dropdown">
            <img src="https://mdbcdn.b-cdn.net/img/Photos/Avatars/avatar-5.webp" alt="avatar"
              class="img-fluid rounded-circle" width="35">
          </div>
          <div area-labelledby="navbarDropdown" class="dropdown-menu" style="right: 0; left: auto;">
            <%= link_to 'Profile', profile_path(current_user), class: 'dropdown-item' %>
            <%= link_to 'Premium', '#', class: 'dropdown-item' %>
            <%= link_to 'Contact', '#', class: 'dropdown-item' %>
            <%= link_to 'Settings', '#', class: 'dropdown-item' %>
            <div class="dropdown-divider"></div>
            <%= button_to 'Sign out', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
          </div>
        </div>

      <% else %>
        <%= link_to 'Sign in', new_user_session_path, class: 'btn btn-primary me-1' %>
        <%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-light' %>
      <% end %>
    </div>
  </div>
</nav>